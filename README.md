# PRIMUM SCRIPTS

**PRIMUM SCRIPTS** - это набор скриптов для автоматической установки необходимого программного обеспчения на Windows и macOS через пакетные менеджеры.

## 📋 Описание

Скрипт предоставляет удобный интерфейс для:
- Сканирования системы при запуске и подсчета программ по статусу
  (установлено через Chocolatey / установлено вне Chocolatey / не установлено)
- Установки только тех программ, которые еще не установлены
  (уже установленное ПО не переустанавливается)
- Просмотра подробных списков программ по статусу
- Проверки и выгрузки в отчет ПО, установленного вне Chocolatey

## 🛠️ Поддерживаемые платформы

- **Windows**: Использует Chocolatey
- **macOS**: Использует Homebrew (в разработке)

## 📦 Структура проекта

```
primum-scripts/
├── install-software.bat        # Запускаемый файл для Windows (установка)
├── install-software.ps1        # Основной PowerShell скрипт установки
├── uninstall-software.bat      # Запускаемый файл для Windows (удаление ПО вне Chocolatey)
├── uninstall-software.ps1      # PowerShell скрипт проверки и удаления ПО вне Chocolatey
├── windows-software.json       # База данных программ для Windows
├── macos-software.json         # База данных программ для macOS
└── README.md                   # Документация

После проверки рядом со скриптами создается файл
non-choco-software-report.txt - отчет о ПО, установленном вне Chocolatey.
```

## ⚡ Быстрый старт

### Windows

1. **Скачайте все файлы** в одну папку
2. **Запустите** `install-software.bat` двойным кликом
3. При запуске скрипт **сканирует систему** и показывает счетчики:
   сколько программ установлено через Chocolatey, сколько вне Chocolatey
   и сколько еще не установлено
4. **Выберите** нужный пункт меню:
   - `1` - Установить неустановленные пакеты (через Chocolatey)
   - `2` - Подробнее (списки по статусу)
   - `3` - Обновить данные (пересканировать систему)
   - `4` - Проверить ПО, установленное вне Chocolatey (создает отчет .txt)
   - `0` - Выход

Для удаления ПО, установленного вне Chocolatey, запустите `uninstall-software.bat`:
   - `1` - Проверка и создание отчета `non-choco-software-report.txt`
   - `2` - Удаление найденных программ через штатные деинсталляторы (с подтверждением)
   - `0` - Выход

### Альтернативный запуск через PowerShell

```powershell
# Перейти в папку со скриптом
cd "путь\к\папке\win-scripts"

# Запустить скрипт
powershell -NoProfile -ExecutionPolicy Bypass -File "install-software.ps1"
```

## 🎯 Функциональность

### Сканирование при запуске
- При каждом запуске скрипт один раз сканирует систему (реестр + `choco list`)
  и делит все программы из списка на три группы:
  установлено через Chocolatey, установлено вне Chocolatey, не установлено
- Счетчики по группам выводятся в главном меню
- Пункт `3` позволяет пересканировать систему в любой момент

### 1. Установить неустановленные пакеты (через Chocolatey)
- Устанавливает через Chocolatey **только те программы, которые не
  установлены** (ни через Chocolatey, ни вне него)
- Уже установленное ПО НЕ переустанавливается — так исключается ситуация,
  когда программа уже стоит, но повторно ставится через Chocolatey
- Программы без `choco_id` (например, Cursor) пропускаются с подсказкой
  скачать вручную с официального сайта
- Автоматически устанавливает Chocolatey при необходимости
- После установки данные пересканируются, счетчики обновляются

### 2. Подробнее (списки по статусу)
- Показывает три раздела с группировкой по категориям:
  установленные через Chocolatey `[+]`, установленные вне Chocolatey `[~]`
  (с отображаемым именем и путем) и не установленные `[-]` (с описанием)

### 4. Проверка ПО, установленного вне Chocolatey
- Сканирует реестр Windows ("Удаление программ"):
  - `HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall`
  - `HKLM\SOFTWARE\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall`
  - `HKCU\Software\Microsoft\Windows\CurrentVersion\Uninstall`
- Программа считается установленной вне Chocolatey, если она найдена
  в реестре, но ее пакет отсутствует в `choco list`
- Создает отчет `non-choco-software-report.txt` с именем, версией,
  путем установки (Install path) и строкой деинсталляции (Uninstall)

### 5. Удаление ПО вне Chocolatey (uninstall-software.bat)
- Использует тот же отчет/проверку, что и пункт 4
- Для каждой найденной программы запрашивает подтверждение (y/n)
- Запускает штатный деинсталлятор:
  - MSI пакеты: `msiexec /x {ProductCode}`
  - Остальные: запуск `uninstall.exe` из строки деинсталляции
- После удаления проверяет реестр и обновляет отчет

## 📊 Категории программ

Скрипт поддерживает следующие категории:

- **🔧 Разработка**: Git, VS Code, Android Studio, Docker, Python, Node.js
- **🌐 Браузеры**: Chrome, Perplexity
- **🎵 Мультимедиа**: VLC, OBS Studio
- **📁 Утилиты**: WinRAR
- **💬 Общение**: Discord, Telegram
- **📝 Продуктивность**: Obsidian, Todoist

## ⚙️ Требования

### Windows
- Windows 10/11
- PowerShell 5.1 или выше
- Подключение к интернету
- Права администратора (для установки Chocolatey)

### Автоматические зависимости
Скрипт автоматически:
- Устанавливает Chocolatey, если не найден
- Настраивает ExecutionPolicy для выполнения
- Обрабатывает ошибки установки

## 🔧 Настройка

### Добавление новых программ

Отредактируйте файл `windows-software.json`:

```json
{
  "name": "Название программы",
  "description": "Описание программы",
  "choco_id": "chocolatey-package-id",
  "official_url": "https://official-website.com/",
  "installer_type": "exe"
}
```

### Изменение категорий

Программы организованы по категориям в JSON файле. Вы можете:
- Добавлять новые категории
- Перемещать программы между категориями
- Изменять описания

## ⚠️ Важные замечания

### 🔤 Кодировка файлов

**КРИТИЧЕСКИ ВАЖНО**: Для корректного отображения русского текста в терминале все файлы должны быть сохранены в кодировке **UTF-16 LE** (UTF-16 Little Endian).

#### Как проверить/изменить кодировку:

**Visual Studio Code:**
1. Откройте файл `.ps1`
2. В нижней панели справа найдите текущую кодировку
3. Нажмите на неё и выберите "Save with Encoding"
4. Выберите "UTF-16 LE"

**Notepad++:**
1. Откройте файл
2. Меню "Кодировки" → "Кодировать в UTF-16 LE"
3. Сохраните файл

**Блокнот Windows:**
1. "Сохранить как"
2. В поле "Кодировка" выберите "Unicode"

### ⚡ Права доступа

При первом запуске может потребоваться:
- Запуск от имени администратора
- Разрешение на выполнение скриптов PowerShell
- Разрешение на установку Chocolatey

## 🐛 Устранение неполадок

### Проблема: Крякозябры в тексте
**Решение**: Убедитесь, что файл `.ps1` сохранен в кодировке UTF-16 LE

### Проблема: Ошибка ExecutionPolicy
**Решение**: Запустите PowerShell от администратора и выполните:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Проблема: Chocolatey не устанавливается
**Решение**: 
1. Проверьте подключение к интернету
2. Запустите PowerShell от администратора
3. Убедитесь, что TLS 1.2 включен

### Проблема: Программа не найдена в Chocolatey
**Решение**: Скрипт предложит ссылку для ручной установки

## 🔄 Обновления

Для обновления списка программ:
1. Отредактируйте `windows-software.json`
2. Перезапустите скрипт
3. Изменения применятся автоматически

## 📝 Логи и отладка

Скрипт выводит подробную информацию о:
- Процессе установки каждой программы
- Ошибках и их причинах
- Статистике установки
- Найденных/ненайденных программах

## 🤝 Участие в разработке

Для добавления поддержки новых программ:
1. Найдите Chocolatey ID программы на [chocolatey.org](https://chocolatey.org/packages)
2. Добавьте запись в соответствующую категорию JSON файла
3. Протестируйте установку
---

**Автор**: MadMarchello  
**Версия**: 1.0  
**Лицензия**: MIT
